import classnames from 'classnames';
import { BaseForm } from 'uniforms';
const Semantic = (parent) => { var _a; return _a = class extends parent {
        getNativeFormProps() {
            const props = super.getNativeFormProps();
            const error = this.getChildContextError();
            return Object.assign(Object.assign({}, props), { className: classnames('ui', props.className, { error }, 'form') });
        }
    },
    _a.Semantic = Semantic,
    _a.displayName = `Semantic${parent.displayName}`,
    _a; };
export default Semantic(BaseForm);
